package pageObjects;
import java.time.Duration;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
public class Home {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*WebDriver driver=new ChromeDriver();
		driver.manage().window().maximize();
		driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(10));
		driver.get("https://qaautomationlabs.com/");
		driver.findElement(By.xpath("(//a[normalize-space()='Shop'])[2]")).click();
		
		driver.findElement(By.xpath("//input[@id='email']")).sendKeys("demo@demo.com");
		driver.findElement(By.xpath("//input[@id='password']")).sendKeys("demo");
		driver.findElement(By.xpath("//button[@id='loginBtn']")).click();*/
		
		

	}

}
